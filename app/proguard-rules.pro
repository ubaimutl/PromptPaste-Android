# Plyph does not use reflection-based model serialization. Android entry
# points declared in the manifest and Compose runtime rules are retained by the
# Android Gradle plugin and their respective libraries.
